name: credit_card_lake
version: 1.0.0
config-version: 2
profile: credit_card_lake

model-paths: [models]
test-paths: [tests]
macro-paths: [macros]

models:
  credit_card_lake:
    silver:
      +materialized: table
      +tags: [silver]
    gold:
      +materialized: table
      +tags: [gold]
